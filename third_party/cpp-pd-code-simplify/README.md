# cpp-pd-code-simplify

This directory vendors the MIT-licensed header-only simplifier from:

```text
https://github.com/GGN-2015/cpp-pd-code-simplify
```

The project uses `include/pdcode_simplify/pdcode_simplify.hpp` as an embedded
PD-code simplification backend. The original license is preserved in
`LICENSE`.
